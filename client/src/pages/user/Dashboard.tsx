import UserMenu from "@/components/admin/UserMenu";

const Dashboard = () => {
  return (
    <div>
      <UserMenu>
        <h2>Dashboard</h2>
      </UserMenu>
    </div>
  );
};

export default Dashboard;

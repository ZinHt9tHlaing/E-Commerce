import UserMenu from "@/components/admin/UserMenu";

const Orders = () => {
  return (
    <section>
      <UserMenu>
        <h4 className="text-2xl font-medium">USER PROFILE</h4>
      </UserMenu>
    </section>
  );
};

export default Orders;
